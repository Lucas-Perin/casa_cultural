<%= form_with(model: analise) do |form| %>
  <% if analise.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(analise.errors.count, "error") %> prohibited this analise from being saved:</h2>

      <ul>
        <% analise.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :filme_id, style: "display: block" %>
    <%= form.number_field :filme_id %>
  </div>

  <div>
    <%= form.label :nota, style: "display: block" %>
    <%= form.number_field :nota %>
  </div>

  <div>
    <%= form.label :analise, style: "display: block" %>
    <%= form.textarea :analise %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
